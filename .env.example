# Servidor
PORT=9002
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Banco (local via docker compose)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pwa_projetofinal?schema=public"

# OAuth Google (crie em Google Cloud Console)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:9002/auth/google/callback

# JWT (chave local; na VPS use valor forte)
JWT_SECRET=troque-esta-chave
JWT_EXPIRES_IN=2h
